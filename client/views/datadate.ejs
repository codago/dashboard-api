<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <link rel='stylesheet' href='/stylesheets/bootstrap.css' />
  <script src="/javascripts/jquery-2.x-git.min.js"></script>
</head>
<body>

  <nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#">Content Management System</a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><a href="/home">Home<span class="sr-only">(current)</span></a></li>
          <li><a href="/data">Data</a></li>
          <li><a href="/datadate">Data Date</a></li>
          <li><a href="/maps">Maps</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li><a href="/logout">Log Out</a></li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>



  <div class="container ">
    <div id="success-add-alert"class="alert alert-success" hidden>
      <strong>Success!</strong> new data has been added to the database
    </div>
    <div id="success-edit-alert"class="alert alert-success" hidden>
      <strong>Success!</strong> data has been edited
    </div>

    <div id="success-delete-alert" class="alert alert-info" hidden>
      <strong>Success!</strong> data has been deleted
    </div>


    <div id="warning-inputnotnumber-alert" class="alert alert-danger" hidden>
      <strong>Warning!</strong> please input only number
    </div>


    <div class="">
      <div id="add-button" class="container">
        <button class="btn btn-primary" type="button" name="button">Add</button>

      </div>
      <br/>
      <div id="control-form-div" class="container" hidden>
        <div class="jumbotron">
          <form id="control-form" class="form-inline" method="post">
            <div class="form-group">
              <label for="">Letter:</label>
              <input id="letter-add" type="date" class="form-control" name="letter" value="" required>
            </div>
            <div class="form-group">
              <label for="">Frequency:</label>
              <input id="frequency-add" type="float" class="form-control" name="frequency" placeholder="0.00000"value="" required>
            </div>
            <button type="submit" class="btn btn-default" href="#">Save</button>
          </form>
        </div>
      </div>


      <h1>Search</h1>
      <hr/>

      <form id="search-form" class="form-inline" method="post">
        <div class="form-group">
          <label for="">Letter:</label>
          <input id="letter-search" type="date" class="form-control" name="letter" value="">
        </div>
        <div class="form-group">
          <label for="">Frequency:</label>
          <input id="frequency-search" type="text" class="form-control" name="frequency" value="">
        </div>
      </form>


      <div class="">
        <table id="datadate-table" class="table table-striped">
          <thead>
            <tr>
              <th>#</th>
              <th>Letter</th>
              <th>Frequency</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>

          </tbody>
        </table>
      </div>

    </div>
  </div>

  <script type="text/javascript">
  var idedit = ""
  $(document).ready(function(){
    drawTable();
    add();
    edit();
    remove();
    save();
    search();
  });

  function hideAlert() {
    if($('#success-add-alert').attr('hidden') !== 'hidden') {$('#success-add-alert').attr('hidden', 'true')}
    if($('#success-edit-alert').attr('hidden') !== 'hidden') {$('#success-edit-alert').attr('hidden', 'true')}
    if($('#success-delete-alert').attr('hidden') !== 'hidden') {$('#success-delete-alert').attr('hidden', 'true')}
    if($('#warning-inputnotnumber-alert').attr('hidden') !== 'hidden') {$('#warning-inputnotnumber-alert').attr('hidden', 'true')}
  }

  function drawTable(searchData) {
    if(!searchData) {
      $.get('http://localhost:3001/api/datadate', function(data){
        let html = '';
        for(let i=0; i<data.length; i++) {
          html += "<tr>"
          html += "<td>" + (i + 1) + "</td>"
          html += "<td>" + data[i].letter + "</td>"
          html += "<td>" + data[i].frequency + "</td>"
          html += "<td><button class='btn btn-success btn-edit' type='button' data-id='" + data[i]._id + "'>Edit</button> &nbsp"
          html += "<button class='btn btn-danger btn-delete' type='button' data-id='" + data[i]._id + "'>Delete</button></td>"
          html += "</tr>"
        }
        $("#datadate-table tbody").html(html);
      });
    } else {
      let html = '';
      if(searchData.length > 0) {
        for(let i=0; i<searchData.length; i++) {
          html += "<tr>"
          html += "<td>" + (i + 1) + "</td>"
          html += "<td>" + searchData[i].letter + "</td>"
          html += "<td>" + searchData[i].frequency + "</td>"
          html += "<td><button class='btn btn-success btn-edit' type='button' data-id='" + searchData[i]._id + "'>Edit</button> &nbsp"
          html += "<button class='btn btn-danger btn-delete' type='button' data-id='" + searchData[i]._id + "'>Delete</button></td>"
          html += "</tr>"
          $("#datadate-table tbody").html(html);
        }
      } else {
          $("#datadate-table tbody").html(html);
      }

    }

  }

  function add() {
    $('#add-button').on('click', '.btn-primary', function(){
      idedit = "";
      // console.log($('#control-form-div')[0].attributes);
      // console.log($('#control-form-div').attr('hidden'));
      if($('#control-form-div').attr('hidden') === 'hidden') {
        hideAlert()
        $('#control-form-div').removeAttr('hidden')
        $('#frequency-add').val('');
        $('#letter-add').val('');
      } else {
        $('#control-form-div').attr('hidden', 'true')
        $('#frequency-add').val('');
        $('#letter-add').val('');
      }
    })
    $('#control-form').on('keyup', '#frequency-add', function(){
      let frequencyAddInput = $('#frequency-add').val()
      let frequencyAddInputLength = $('#frequency-add').val().length
      if(frequencyAddInput[frequencyAddInputLength-1].match(/^[a-zA-Z]*$/) !== null) {
        $('#warning-inputnotnumber-alert').removeAttr('hidden')
        $('#frequency-add').val('')
      } else {
        hideAlert()
      }
    })
  }

  function save() {
    $('#control-form').submit(function(e){
      e.preventDefault();
      if(idedit !== "") {
        $.ajax({
          method: "PUT",
          url: "http://localhost:3001/api/datadate/" +  idedit,
          data: {
            letter: $(this).serializeArray()[0].value,
            frequency: $(this).serializeArray()[1].value
          }
        }).done(function(data){
          idedit = "";
          $('#frequency-add').val('');
          $('#letter-add').val('');
          $('#control-form-div').attr('hidden', 'false')
          hideAlert()
          $('#success-edit-alert').removeAttr('hidden')
          drawTable();
        })
      } else {
        $.ajax({
          method: "POST",
          url:  "http://localhost:3001/api/datadate/",
          data: {
            letter: $(this).serializeArray()[0].value,
            frequency: $(this).serializeArray()[1].value
          }
        }).done(function(data){
          idedit = "";
          $('#frequency-add').val('');
          $('#letter-add').val('');
          $('#control-form-div').attr('hidden', 'false')
          hideAlert()
          $('#success-add-alert').removeAttr('hidden')
          drawTable();
        })
      }
    })
  }

  function edit() {
    $('#datadate-table tbody').on('click', '.btn-edit', function(){
      $.get('http://localhost:3001/api/datadate/' + $(this).attr("data-id"), function(data){
        idedit = data.data._id;
        if($('#control-form-div').attr('hidden') === 'hidden') {
          $('#control-form-div').removeAttr('hidden')
          $('#frequency-add').val(data.data.frequency);
          $('#letter-add').val(data.data.letter);
          idedit = data.data._id;
        } else {
          $('#frequency-add').val(data.data.frequency);
          $('#letter-add').val(data.data.letter);
          idedit = data.data._id;
        }
      })
    })
  }

  function remove() {
    $('#datadate-table tbody').on('click', '.btn-delete', function() {
      $.ajax({
        method: "DELETE",
        url: "http://localhost:3001/api/datadate/" +  $(this).attr("data-id")
      }).done(function(data) {
        hideAlert()
        $('#success-delete-alert').removeAttr('hidden')
        drawTable()
      })
    })
  }

  function search(){
    $('#search-form').on('keyup', '#frequency-search', function(){
      hideAlert()
      // console.log("ini tanggal:", typeof $('#letter-search').val());
      let frequencySearchInput = $('#frequency-search').val()
      let frequencySearchInputLength = $('#frequency-search').val().length
      if(frequencySearchInput.length === 0) {
        drawTable()
      }
      if(frequencySearchInput[frequencySearchInputLength-1].match(/^[a-zA-Z]*$/) !== null) {
        $('#warning-inputnotnumber-alert').removeAttr('hidden')
        $('#frequency-search').val('')
      }
      $.ajax({
        method: 'POST',
        url: "http://localhost:3001/api/datadate/search",
        data: {
          letter: $('#letter-search').val(),
          frequency: Number(frequencySearchInput)
        }
      }).done(function(data){
        console.log(data);
        drawTable(data);
      })
    })
    $('#search-form').change(function(){
      console.log($('#letter-search').val());
      let frequency = $('#frequency-search').val()
      if(frequency !== '') {frequency = Number(frequency)}
      console.log(frequency);
      $.ajax({
        method: 'POST',
        url: "http://localhost:3001/api/datadate/search",
        data: {
          letter: $('#letter-search').val(),
          frequency: frequency
        }
      }).done(function(data){
        console.log(data);
        drawTable(data);
      })
    })

  }

  </script>

</body>
</html>
